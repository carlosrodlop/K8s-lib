OperationCenter:
  Protocol: ${protocol}
  Hostname: ${hostname}
  Ingress:
    Class: ${ingressClass}
  Casc:
    ControllerBundleStorageSCM: ${c_casc_repo}
  Credentials:
    Github:
      Token: ${secretGHToken}
      User: ${secretGHUser}
ManagedControllers:
  loadCount: ${nLoadControllers}
Agents:
  Namespace: ${agentNamespace}
